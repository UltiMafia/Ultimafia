// Mirror of the `lab` block in data/constants.js. Keep in sync if the
// backend values change.
export const Lab = {
  rankUpPlays: 10,
  graduatePlays: 30,
  poolTenureDays: 30,
  graduateRewardCoins: 100,
  submissionMaxPlays: 30,
};
